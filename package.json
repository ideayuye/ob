{
  "name": "ob",
  "version": "1.0.0",
  "description": "object observe",
  "main": "index.js",
  "scripts": {
    "rollup": "rollup -c config.js -m inline",
    "dev": "rollup -c config.js -w -m inline ",
    "build": "set NODE_ENV=prd&&rollup -c config.js"
  },
  "keywords": [
    "observe",
    "object"
  ],
  "author": "tb",
  "license": "ISC",
  "dependencies": {
    "rollup": "^0.37.0",
    "rollup-plugin-buble": "^0.15.0",
    "rollup-plugin-uglify": "^1.0.1"
  }
}
